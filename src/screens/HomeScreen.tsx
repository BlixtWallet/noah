import { View, Text } from "react-native";

const HomeScreen = () => (
  <View className="flex-1 items-center justify-center bg-white">
    <Text className="text-xl font-bold text-green-500">Home Screen</Text>
  </View>
);

export default HomeScreen;
