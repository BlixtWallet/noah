name: Maestro Test iOS

on:
  pull_request:
    paths:
      - "client/**"
      - "scripts/**"
      - "package.json"
      - "bun.lock"
      - "!**/*.md"
      - "!**/.github/workflows/*"

jobs:
  build-release-ios:
    name: Build iOS Release App
    uses: ./.github/workflows/noah-build-release-ios.yml

  maestro-test-ios:
    name: Maestro Test iOS
    needs: build-release-ios
    runs-on: macOS
    timeout-minutes: 15
    permissions:
      checks: read
      contents: read
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 📥 Download iOS app artifact
        uses: actions/download-artifact@v4
        with:
          name: ios-release-app
          path: ios-release-build/

      - name: 📦 Install Maestro
        run: |
          MAESTRO_VERSION=1.38.1
          curl -fsSL "https://get.maestro.mobile.dev" | bash
          echo "$HOME/.maestro/bin" >> $GITHUB_PATH

      - name: 🔧 Setup Regtest Environment
        run: ./scripts/ark-dev.sh setup-everything

      - name: 📱 Boot iOS Simulator (iPhone 16)
        run: |
          DEVICE_ID=$(xcrun simctl list devices available | grep "iPhone 16" | head -n 1 | awk -F "[()]" '{print $2}')
          if [ -z "$DEVICE_ID" ]; then
            echo "No available simulator found for the criteria."
            exit 1
          fi

          echo "Booting iPhone 15 simulator with DEVICE_ID=$DEVICE_ID..."
          xcrun simctl boot "$DEVICE_ID" || true

          TIMEOUT=120
          START_TIME=$(date +%s)
          echo "Waiting for simulator to boot..."

          while true; do
            CURRENT_TIME=$(date +%s)
            ELAPSED_TIME=$((CURRENT_TIME - START_TIME))

            if [ $ELAPSED_TIME -ge $TIMEOUT ]; then
              echo "Timeout: simulator failed to boot for $TIMEOUT seconds."
              exit 1
            fi

            STATUS=$(xcrun simctl list | grep "$DEVICE_ID" | grep "(Booted)")
            if [[ -n "$STATUS" ]]; then
              echo "Simulator is booted!"
              break
            else
              echo "Still waiting for the simulator to boot..."
              sleep 5
            fi
          done

      - name: 🧪 Run Maestro tests
        run: nix develop .# --command bash -c "xcrun simctl install booted ios-release-build/ios-release.app && bun client maestro:test"
        env:
          MAESTRO_DRIVER_STARTUP_TIMEOUT: 120000

      - name: 🧹 Uninstall iOS app and erase data
        if: always()
        run: |
          BUNDLE_ID="com.anonymous.noah.regtest"

          echo "Terminating app: $BUNDLE_ID"
          xcrun simctl terminate booted "$BUNDLE_ID" 2>/dev/null || true

          echo "Getting app data directory..."
          APP_DATA_DIR=$(xcrun simctl get_app_container booted "$BUNDLE_ID" data 2>/dev/null || echo "")

          if [ -n "$APP_DATA_DIR" ] && [ -d "$APP_DATA_DIR" ]; then
            echo "Clearing app data directory: $APP_DATA_DIR"
            rm -rf "$APP_DATA_DIR" || true
          else
            echo "App data directory not found or already cleared"
          fi

          echo "Uninstalling app: $BUNDLE_ID"
          xcrun simctl uninstall booted "$BUNDLE_ID" || true

      - name: 🧹 Teardown Regtest Environment
        if: always()
        run: ./scripts/ark-dev.sh down

      - name: 📤 Store debug output if failed
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: maestro-debug-output-ios
          path: client/maestro-debug-output/**/*
          include-hidden-files: true
          overwrite: true

  maestro_test_gate:
    name: Maestro Test iOS Gate
    runs-on: ubuntu-latest
    needs: maestro-test-ios
    if: always()
    steps:
      - name: ✅ Gate
        run: |
          if [[ "${{ needs.maestro-test-ios.result }}" == "success" || "${{ needs.maestro-test-ios.result }}" == "skipped" ]]; then
            echo "Maestro tests passed or skipped"
            exit 0
          else
            echo "Maestro tests failed"
            exit 1
          fi
